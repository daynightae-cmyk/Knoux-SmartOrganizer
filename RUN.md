# Run Guide

1. Read PROJECT_STATUS.md and the project README.
2. Use the package manager implied by the lockfile when present.
3. Install dependencies in an isolated environment.
4. Run dev, test, lint, and build scripts in that order.

Known scripts: start,build,install-deps,dev
